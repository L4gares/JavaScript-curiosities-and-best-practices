const name = `rebeca`
const concatenacao = `Hello ` + name + `!`

console.log(concatenacao)

console.log()

const name2 = `rebeca` //com o template tem como fazer algumas coisas que da forma normal nao tem como
const template = `Hello 
${name} 
!`

console.log(template)
console.log()

//expressoes...
console.log(`1 + 1 = ${1 + 1}`)

const up = text => text.toUpperCase()//criando uma funcao 
console.log(`Eii...  ${up(`cuidado`)}!`)
 












